params {
    interproResults = "$baseDir/data/iprscan/"
    proteomes       = "$baseDir/data/fastas/"
    outputDir       = "$launchDir/output"
    taxonIdFile     = "$baseDir/data/taxonIdFile.tsv"
    rulesheet       = "$baseDir/bin/rulesheet.tsv"
}

docker {
    enabled = true
}
